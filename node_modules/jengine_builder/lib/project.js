var windows = require('./windows.js'),
    U       = require('./utils.js'),
    path    = require('path'),
    options = require('./options.js')  
;

var Project = module.exports = U.object.extend({
  optionsObject: options.Project 
},{
  load: function(_path) {
    return new Project({path: _path});
  }
});