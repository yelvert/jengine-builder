var WebGL = module.exports = JEngine.Renderers.Core.extend({
  setContext: function() {
    this.context = this.engine.canvas.getContext('webgl') || this.engine.canvas.getContext('experimental-webgl');
    return this;
  }
});