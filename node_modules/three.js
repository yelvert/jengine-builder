var fs = require('fs');
var path = require('path');
var file = fs.readFileSync(path.join(process.cwd(), 'node_modules', 'threejs', 'build', 'three.js'));
eval(file.toString());
var file = fs.readFileSync(path.join(process.cwd(), 'node_modules', 'threejs-stats', 'src', 'Stats.js'));
eval(file.toString());

module.exports = {THREE: THREE, Stats: Stats};