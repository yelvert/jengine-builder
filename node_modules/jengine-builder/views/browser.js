module.exports = Backbone.View.extend({
  el: '#browser',

  template: requireTemplate('./browser'),

  initialize: function() {
    _.bindAll(this, 'render');
    this.$el.data({view: this});
  },

  render: function() {
    this.$el.html(this.template());
    return this;
  }
})