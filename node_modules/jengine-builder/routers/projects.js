module.exports = Backbone.Router.extend({
  routes: {
    'project/select': 'projectSelection'
  },

  initialize: function () {
    _.bindAll(this, 'projectSelection');
  },

  projectSelection: function () {
    (new JEngineBuilder.Views.Projects.Select()).render();
  }
});