module.exports = Backbone.Router.extend({
  routes: {
    '': 'projectSelection'
  },

  initialize: function () {
    _.bindAll(this, 'projectSelection');
  },

  projectSelection: function () {
    (new JEngineBuilder.Views.Projects.Selection()).render();
  }
});