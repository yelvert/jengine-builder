module.exports = Backbone.Router.extend({
  routes: {
    '': 'index'
  },

  initialize: function () {
    _.bindAll(this, 'index');
  },

  index: function () {
    var project = localStorage.get('project');
    project && (project = Application.loadProject(project));
    if (project) {
      Application.Project = project;
      Application.ActiveView = (new JEngineBuilder.Views.Main).render();
    } else {
      this.navigate('project/select', {trigger: true});
    }
  }
});