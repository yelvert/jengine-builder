var winston = require('winston'),
  path = require('path');

module.exports = new (winston.Logger)({
  transports: [
    new (winston.transports.File)({filename: path.join(process.cwd(), 'debug.log'), colorize: true})
  ]
});